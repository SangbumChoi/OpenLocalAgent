# GENERATED by scripts/build_ablation_configs.py from configs/pretrain/la-93m.yaml
# Arm: 93m-s3. Second seed replicate, so the spread is an estimate rather than a single difference.
# Changed in this stage: seed=13
# Everything else is inherited on purpose - edit the base tier, not this.
# Stage 1 of the ladder for la-93m: language modelling on open text.
#
# Corpus is data/shards/pt-big - FineWeb-Edu (260.1M tokens, ODC-BY-1.0), Cosmopedia v2 (69.9M,
# Apache-2.0) and permissive Python (135.5M, MIT/Apache/BSD/ISC): 460.7M train tokens, every
# source openly licensed. Bound to the 16K BPE tokenizer the shards were packed with.
#
# Global batch is held at 64 rows x 2048 = 131,072 tokens per update across every tier, so the
# ladder varies capacity and step count and not the batch. 16000 steps = 2.097B tokens
# = 22.5 tokens per parameter.
stage: pretrain
model_config: configs/model/la-93m.yaml
data:
  shards_dir: data/shards/pt-big
  tokenizer:
    kind: bpe
    path: data/tokenizer-h100-16k.json
optim:
  name: adamw
  lr: 3.0e-4
  weight_decay: 0.1
  grad_clip: 1.0
schedule:
  type: wsd
  warmup_steps: 320
  total_steps: 16000
  decay_frac: 0.2
batch:
  micro_batch_size: 8
  grad_accum_steps: 8
runtime:
  device: auto
  dtype: auto
  seed: 13
  resume: true
log:
  out_dir: results/runs/pretrain-la-93m-s3
  ckpt_every: 2000
  eval_every: 2000
  eval_batches: 32
