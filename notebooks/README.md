# Notebooks

| Notebook | What it is |
|---|---|
| [`training_101.ipynb`](training_101.ipynb) | One full session end to end — pretrain, midtrain, posttrain, score, read the curves. Runs at toy scale on a Colab T4 or on CPU, using the same commands a real run uses. |

`openlocalagent_pretraining_colab.ipynb` was removed: it covered pretraining only, and pinned a model
config (`webgpu-35m-hybrid`) that no longer exists. Recoverable from the `pre-refactor` tag.
